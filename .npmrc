# pnpm v10 supply-chain policy: minimumReleaseAge deaktiviert,
# da Template-Deps explizit gewählt sind (kein automatisches Update-Bot-Risiko).
# Für Produktionsprojekte auf Wunsch auf '3d' setzen.
minimum-release-age=0
